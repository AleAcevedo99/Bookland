<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bookland">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bookland">
        <activity android:name=".AboutUsActivity"></activity>
        <activity android:name=".AdvancedSearchResultsActivity" />
        <activity android:name=".BookShelfsActivity" />
        <activity android:name=".AdvancedSearchActivity" />
        <activity android:name=".CatalogShelfsActivity" />
        <activity android:name=".RecommendationsActivity" />
        <activity android:name=".BookDetailActivity" />
        <activity android:name=".ShelfActivity" />
        <activity android:name=".FavoriteAuthorsActivity" />
        <activity android:name=".FavoriteGendersActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>